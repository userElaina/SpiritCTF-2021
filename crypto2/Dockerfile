#===========================
# 使用方式 在本目录下输入 docker build -t badmonkey/crypto1 .
# 默认端口设置为10000 运行镜像方式 docker run -d -p 10000:10000 --name crypto1 badmonkey/crypto1
# 端口映射可以根据需求改动
#===========================

#===========================
# 标准镜像，支持ubuntu和centos
#===========================
FROM furritos/pycryptodome:3.7-alpine


#===========================
# 拷贝同目录下的文件到容器中
#===========================
COPY /src /CRYPTO

#===========================
# 开放端口号，默认为10000
#===========================
EXPOSE 10000

WORKDIR /CRYPTO
#===========================
# 启动服务器脚本
#===========================
CMD python /CRYPTO/task.py

